appId: com.lop.budget
name: "TC-42-7: Ajouter une date de fin à la série"
tags:
  - recurrence
  - edition
---
- runFlow: ../../.maestro/subflows/bootstrap_monthly.yaml

- evalScript: |
    console.log("Scenario: Ajouter une date de fin à la série")

# Given l'utilisateur ouvre l'édition de l'occurrence "Loyer" du mois courant
# And il sélectionne la portée "Toute la série"
- runFlow:
    file: ../../.maestro/subflows/navigate_to_edit.yaml
    env:
      transactionName: "Loyer"
      scopeSelection: "Toute la série"

# When il renseigne la date de fin {dateFin} (Fin du mois courant)
# On choisit le 28 pour être sûr d'être dans le mois courant
- runFlow:
    file: ../../.maestro/subflows/update_recurrence_end_date.yaml
    env:
      target_day: "28"

# And il enregistre
- tapOn: { id: "common.btn.save" }

# Then l'occurrence "Loyer" est toujours affichée sur le mois courant
- assertVisible: "Loyer"

# And aucune occurrence "Loyer" n'est affichée sur le mois suivant
- runFlow:
    file: ../../.maestro/subflows/change_month.yaml
    env:
      month_name: ${output.NEXT_MONTH_NAME}
      year: ${output.NEXT_YEAR}
- assertNotVisible: "Loyer"
